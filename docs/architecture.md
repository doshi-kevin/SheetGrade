# Architecture

Pipeline: `parse → detect regions → semantically label → ALIGN → compare → score → diagnose → review`

To be filled in as each phase lands. See [PROJECT_PLAN.md](PROJECT_PLAN.md) for the phase breakdown.
